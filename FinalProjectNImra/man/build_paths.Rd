% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_paths.R
\name{build_paths}
\alias{build_paths}
\title{Multi-Path Forward Selection}
\usage{
build_paths(
  X,
  response,
  K = 5,
  epsilon = 1e-06,
  delta = 2,
  L = 25,
  model_type = NULL
)
}
\arguments{
\item{X}{Data frame of predictors and response.}

\item{response}{Response variable name (character).}

\item{K}{Maximum number of forward steps (default 5).}

\item{epsilon}{Minimum AIC improvement to continue from a parent (default 1e-6).}

\item{delta}{AIC tie threshold; keep children within `best + delta` (default 2).}

\item{L}{Maximum number of models to keep per step (default 25).}

\item{model_type}{"linear", "logistic", or `NULL` for auto-detect (default `NULL`).}
}
\value{
A \code{list} `path_forest` containing:
\describe{
return(list(
  \item{frontiers}: list of frontiers (models kept at each step)
  \item{aic_by_model}: named list containing AIC values for retained models
  \item{meta}: list of meta settings (K, epsilon, delta, L, model_type)
}
}
\description{
Implements forward selection with multiple near-optimal AIC paths for
linear (Gaussian) or logistic (binomial) regression. If `model_type = NULL`,
the type is auto-detected from the response:
- Binary factor / logical / numeric in {0,1} → `"logistic"`
- Otherwise → `"linear"`
}
\examples{
# Linear (auto-detected): response is numeric with many values
mp_lin <- build_paths(mtcars, response = "mpg", K = 3, delta = 2)
# Logistic (auto-detected): response has 2 levels
mt <- within(mtcars, { am <- factor(am) })
mp_log <- build_paths(mtcars, response = "am", K = 3, delta = 2)
}
